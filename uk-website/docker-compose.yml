services:
  website:
    container_name: dev-company-website

    build:
      context: .

    image: dev-company-website

    expose:
      - "80"

    restart: unless-stopped

    networks:
      - dev-network

networks:
  dev-network:
    external: true
